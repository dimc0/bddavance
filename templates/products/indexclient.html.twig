{% extends 'base.html.twig' %}

{% block title %}Products index{% endblock %}

{% block body %}
    <h1>Products index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Description</th>
            <th>Categorie</th>
        </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.name }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.category.name }}</td>
                <td><a href="" title="Ajouter au panier">🛒</a></td>

            </tr>
        {% else %}
            <tr>
                <td colspan="6">Pas de produits disponibles</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

