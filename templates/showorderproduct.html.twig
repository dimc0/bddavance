{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>order Details</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ order.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ order.name }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ order.price }}</td>
        </tr>
        <tr>
            <th>Stock</th>
            <td>{{ order.stock }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ order.description }}</td>
        </tr>
        <tr>
            <th>Category Id</th>
            <td>{{ order.category ? order.category.id : 'N/A' }}</td>
        </tr>
        <tr>
            <th>Category Name</th>
            <td>{{ order.category ? order.category.name : 'N/A' }}</td>
        </tr>
        {% if order.productsOrders|length > 0 %}
            <tr>
                <th>Orders</th>
                <td>
                    <ul>
                        {% for po in order.productsOrders %}
                            <li>Order #{{ po.order.id }} — Quantity: {{ po.quantity }}</li>
                        {% endfor %}
                    </ul>
                </td>
            </tr>
        {% else %}
            <tr>
                <th>Orders</th>
                <td>No orders for this product.</td>
            </tr>
        {% endif %}
        </tbody>
    </table>

    <a href="{{ path('app_products_index') }}">Back to list</a>
    <a href="{{ path('app_products_edit', {'id': order.id}) }}">Edit</a>

    {{ include('products/_delete_form.html.twig') }}
{% endblock %}
